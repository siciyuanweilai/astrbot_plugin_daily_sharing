### v5.4.6
**🎨 通用生图插件适配**

*   **新增生图提供者配置**：`image_provider` 支持 `gitee_aiimg`、`generic_plugin` 与 `auto`；默认保持原有万象画卷适配，避免影响现有用户。
*   **通用插件方法调用**：新增 `generic_image_plugin_name`、`generic_image_method_path`、`generic_image_prompt_arg`、`generic_image_extra_args` 与 `generic_image_result_field`，可将每日分享生成的配图 Prompt 传给其它生图插件暴露的方法，并自动识别常见返回字段中的图片路径或 URL。
*   **自动扫描生图方法**：新增 `auto_scan` provider，并增强 `auto` 模式；当未找到万象画卷且未指定通用插件方法时，会扫描已加载插件里常见的 `generate_image`、`draw_image`、`txt2img`、`draw.generate` 等方法并自动尝试调用。
*   **保留万象画卷专属能力**：形象参考图、图生图与视频生成仍走 `astrbot_plugin_gitee_aiimg` 专用路径；切换到通用 provider 时会自动跳过参考图能力，保证普通文生图兼容性。

### v5.4.5
**🧠 AstrBot 上下文、会话模型、平台历史与触发反馈修复**

*   **会话级 LLM Provider 对齐 AstrBot**：插件未单独指定 `llm_provider_id` 时，会优先使用 AstrBot 当前目标会话的聊天模型 Provider；只有读取不到时才使用系统默认模型。内容生成、配图 Agent、视频 Agent与 TTS 情感分析都会尽量沿用目标会话配置。
*   **平台消息历史兜底增强**：非 OneBot 平台或 OneBot 历史拉取失败时，优先读取 AstrBot 保存的 `message_history_manager` 平台消息记录，再兜底读取 `conversation_manager` LLM 会话历史；WebChat 会同时尝试完整 session id 与裸 session id。
*   **主动分享历史不再污染上下文**：主动分享写入 AstrBot 历史时改用内部触发标记；读取历史时会过滤该标记，并把紧随其后的 Bot 主动分享降级为“背景：你之前主动分享过……”，避免把主动分享伪装成用户真实请求，也避免计入群聊热度。
*   **LLM 工具触发反馈更直观**：通过自然语言触发 `daily_share` 时，会尽量给原消息贴上“处理中 / 成功 / 失败”表情；平台不支持 `set_msg_emoji_like` 或缺少 `message_id` 时自动跳过，不阻断分享流程。
*   **QQ空间微信回发更稳**：从 `weixin_oc` 手动触发 QQ 空间分享后，同步回当前微信会话的图片会走插件已有的微信图片压缩与 60 秒发送超时逻辑，并优先使用更小的回执副本；若微信 CDN 仍拒收图片，会再重试一次，重试失败时保留文字回发，不影响 QQ 空间已发布状态。
*   **跨平台目标识别更稳**：带完整适配器 ID 的非 OneBot UMO 不再因为真实 ID 是数字而误走 OneBot 历史拉取；定时发送会优先按目标适配器查找平台实例，减少多平台共存时的误判。

### v5.4.0
**🧭 任务系统模块化、早报随机延迟与发送链路修复**

*   **任务系统按职责拆分**：将原本集中在 `core/tasks.py` 的任务逻辑拆分为 `core/tasks/` 包，按 `scheduler`、`targets`、`executor`、`delivery`、`cache`、`manager` 等模块组织；`TaskManager` 对外入口保持一致，便于后续维护、测试和定位定时链路问题。
*   **早报定时支持随机延迟**：新增 `briefing_cron_random_delay` 配置项，早报 Cron 触发后可在 `0-N` 分钟内随机发送；默认 `0` 表示准点发送。
*   **早报延迟任务可恢复**：早报随机延迟会写入 SQLite 的 `briefing.pending_delay_job`；Bot 重启后会恢复尚未执行的早报任务，近期错过的早报会自动补偿执行，过期太久的记录会自动清理。
*   **关闭任务时清理更完整**：清理待执行延迟任务时同步清理普通分享、QQ空间和早报记录，避免停用自动分享后旧早报任务意外补发。
*   **手动早报广播更稳**：`/分享 60s 广播`、`/分享 ai 广播` 现在会先下载长图到本地，再按目标平台准备图片并逐个发送；执行结束会回报成功/失败数量，单个目标失败不再中断整轮广播。
*   **图生视频提示词更智能**：视频生成会根据配图描述和分享文案分别生成动态提示词与声音提示词，保留原图人物、服装、场景一致性，同时让镜头运动、动作细节、环境声和音乐氛围更贴合内容。
*   **内容库与搜索配置更干净**：内容库解析同时兼容 WebUI 的 `List[str]` 和内置默认 `Dict`；Web 搜索提供商与 Key 改为从 AstrBot 运行时配置读取，减少本地配置文件和环境变量 fallback。

### v5.3.6
**🧩 新闻接口更新与指令体验修复**

*   **新闻接口更新与解析增强**：新闻源统一使用新版柠柚 API v2 路径；新闻响应解码支持从带 BOM、状态前缀等非标准采样文本中提取 JSON payload。
*   **新闻失败不再静默兜底**：普通会话、手动触发与 QQ 空间新闻分享在新闻源全部失败时会明确取消，并写入失败历史，避免 LLM 在缺少新闻数据时继续生成无依据内容。
*   **内容前缀可配置**：新增 `show_knowledge_type_prefix`、`show_rec_type_prefix`，可分别关闭知识和推荐正文前的类型前缀。
*   **命令参数校验更严格**：非新闻类型现在只接受 `广播`、`空间` 后缀；多余参数会给出“无效参数”提示，避免用户误以为 `/分享 推荐 动漫` 已指定主题。

### v5.3.5
**🧹 个人微信压缩图片临时文件清理优化**

*   **个人微信压缩图按数量清理**：`weixin_oc` 发送前生成的 `Temp/weixin_send_*.jpg` 改为默认最多保留最新 10 张，超过后删除最旧文件；启动、每日任务和生成新压缩图后都会触发清理。
*   **新增配置项**：`weixin_temp_cleanup_max_count` 控制个人微信压缩图保留张数，默认 `10`，设为 `0` 可关闭自动清理。
*   **未使用副本即时删除**：如果压缩后的 JPEG 副本不比原图更小，会立即删除该副本并继续发送原图，减少无效临时文件堆积。

### v5.2.5
**🧠 新闻背景获取策略优化**

*   **API 摘要优先**：热搜解析器现在会读取新闻源返回的 `description`、`desc`、`summary`、`content`、`detail` 等字段，优先作为真实背景交给 LLM。
*   **搜索降级为兜底**：新闻源已提供可用摘要/正文时，将跳过 Tavily / Brave；只有缺少背景的条目才补充 Web 检索，减少搜索配额消耗并提升速度。
*   **字段兼容增强**：补充支持 `hot_value_desc`、`score_desc`、`mobile_url`、`mobile_link` 等热度和链接字段。

**🔗 新闻长图原文链接反查**

*   **长图同步缓存 JSON**：发送新闻热搜长图时，会缓存同源新闻列表，直接用 API 返回的标题与链接建立序号映射。
*   **支持序号取链**：可直接说“第3条链接”，或引用长图说“第3条”，由 LLM 获取对应新闻原文链接。
*   **同源保护**：长图场景获取缓存时禁用备用源降级，避免“图片是微博，链接却来自其它源”的错位。

### v5.2.1
**🤝 个人微信适配与跨平台发送链路完善**

**1. 🟢 weixin_oc 个人微信适配**
*   **正式支持个人微信私聊目标**：接收对象支持填写 `/sid` 输出的纯 `UID` / `Session ID`，例如个人 QQ 填 `123456`，个人微信填 `o2...@im.wechat`。
*   **主动发送走 AstrBot 原生链路**：个人微信定时分享、早报分享改为使用 AstrBot `send_by_session` 主动发送，不再依赖插件运行期缓存的手动触发事件。
*   **平台识别为精确 `weixin_oc`**：个人微信专属发送、图片优化和 `context_token` 检查只对平台名或适配器元信息精确为 `weixin_oc` 的适配器启用，避免误命中公众号、企业微信等其它微信系适配器。
*   **context_token 提示更清晰**：当 weixin_oc 主动发送目标缺少 AstrBot 保存的 `context_token` 时，会给出明确日志提示：让该微信私聊先给机器人发任意消息，以刷新 `weixin_oc_context_tokens`。

**2. ⚙️ 指令与配置体验优化**
*   **`/分享 添加当前` 支持纯 ID 写入**：在 QQ 私聊执行会保存 QQ 号，在个人微信私聊执行会保存微信 openid；`/分享 添加当前 早报` 同样适用于早报接收对象。
*   **配置说明对齐 `/sid`**：README 与配置提示统一说明通过 `/sid` 获取目标 ID；私聊复制 `UID`/`Session ID`，群聊复制 `Session ID`。
*   **自定义用户称呼映射更轻量**：QQ/OneBot 私聊优先通过接口获取备注或昵称；个人微信优先使用 AstrBot 人格设定里的用户称呼，本地 `contact_aliases` 仅作为日程/记忆识别用户称呼的补充。

**3. 🖼️ 个人微信发送图片稳定性**
*   **发送前高质量优化副本**：weixin_oc 发送图片前会生成一份用于发送的高质量 JPEG 副本，默认保留 4K 最长边，并按目标大小控制文件体积，降低 CDN 上传超时概率。QQ 与 QQ空间不受影响。
*   **配置口径调整**：个人微信图片目标大小使用 KB 字段 `weixin_image_max_size_kb`，默认 `10240KB`；个人微信上传超时使用秒字段 `weixin_api_timeout_seconds`，默认 `60` 秒。

### v5.2.0
**🌟 QQ空间生态稳定性增强：登录态无感自动保活**

**1. 🛡️ 彻底解决 Cookie 过期导致的发布失败**
*   **安全发布机制**：重构了向 QQ 空间发布说说的底层调用链路。精准捕获了因 Cookie 过期导致的特定底层报错（如图片上传接口返回 `-100`、`-3000` 或抛出“登录失效”等前置校验未能涵盖的异常）。
*   **无感刷新与自动重试**：当检测到登录态失效时，系统现在会主动清空底层空间插件的 Cookie 缓存，并隐式通过查询指令强制唤醒底层的重新登录和 Cookie 刷新流程。获取最新授权后，系统会自动进行二次发布重试。
*   **体验提升**：彻底告别因 QQ 空间风控或 Cookie 自然过期导致定时说说发送中断的问题，实现 QQ 空间分享任务的真正无人值守与极高可用性。

### v5.1.9
**🖼️ 发送图片稳定性与存储规范升级**

**1. 🛡️ 严格规避发送报错**
*   **拒绝不稳定链路**：全面重构了底层的图片发送逻辑。由于底层 QQ 机器人框架（NapCat）在处理不稳定 CDN 链接时极易触发 `富媒体传输失败` 甚至导致整条消息发送失败，现在插件对群聊、私聊**强制**将图片下载到本地后再发送。

**2. 📁 临时文件存储规范化**
*   **独立 Temp 文件夹**：所有临时下载的图片都会被自动归类，并统一储存于独立的 `Temp` 文件夹中，保持插件根目录的整洁。

### v5.1.7
**🗣️ 社交与称呼优化：优先读取用户备注**

**1. 🏷️ 称呼更具“人情味”**
*   **备注优先机制**：在私聊场景及大模型提取用户称呼时，底层逻辑现已改为**优先使用QQ平台设置的“用户备注”**。只有在没有设置备注的情况下，才会退回使用“用户昵称”。
*   **体验提升**：彻底解决机器人总是用冗长或中二的网名来称呼你的“生分感”，让每日分享和专属问候的语气更贴近真实好友间的交互。

### v5.1.6
**⏱️ 定时调度增强：全面兼容高阶 Cron 表达式**

**1. ⚙️ 突破常规位数限制**
*   **兼容 5/6/7 位 Cron**：重构了底层的定时任务解析器，彻底移除了硬编码的 5 位格式限制。现在完美支持标准的 5 位（分、时、日、月、周）、Spring 风格的 6 位（精确到秒）以及 7 位（带年）的完整 Cron 表达式。
*   **秒级精准调度**：无论你是习惯用 `0 8 * * *` 还是 `0 0 8 * * *`，插件都能精准使用参数列并正确挂载任务，彻底解决填入带“秒”的 Cron 表达式导致的报错和跳过问题。

### v5.1.5
**🌐 搜索引擎生态扩充：全面支持 Brave 联网检索与高可用优化**

**1. 🔍 双擎搜网无缝切换**
*   **Brave 联网检索接入**：底层 Web 检索能力重构，现已打通 AstrBot 核心配置。系统会自动侦测你配置的 `websearch_provider`，智能切换使用 **Tavily** 或 **Brave**。无论你偏好哪个引擎，Bot 都能带着脑子吃瓜。
*   **高可用轮询**：为 Tavily 或 Brave 搜索全面接入了“多 Key 失败轮询”与“防超额重试”机制，即使单个 API Key 耗尽也能无感切换，检索稳定性拉满。

**2. 🛠️ 健壮性与细节修复**
*   **异常流拦截**：修复了极端情况下（如 Brave API 节点返回 Null/空值时），导致机器人底层序列化报错及消息流崩溃的严重 Bug。
### v5.1.2
**🔍 资料准确性与防幻觉升级：全面接入 Tavily 联网检索**

**1. 🌐 全域联网增强**
*   **彻底告别标题党**：打通 AstrBot 内置的 Tavily 网页搜索能力。开启后，在分享新闻热搜时，Bot 会自动去全网检索事件的“最新进展”与“真实细节”，拒绝仅凭标题进行苍白的脑补。
*   **双引擎并发检索**：重构了【知识】和【推荐】的底层链路。现在会采用“百度百科 + Tavily 搜索”双引擎并发获取参考资料。不仅大幅丰富了科普原理和作品评价，更从根本上降低了模型产生“幻觉”的概率。
*   **智能 Query 路由**：Agent 会根据当前生成的类型（新闻/知识/推荐），动态调整搜索关键词（如知识类侧重“原理、详细解释”，推荐类侧重“评价、核心亮点”）。

**2. ⚙️ 配置化管理**
*   在 `新闻热搜设置` 中新增 `启用 Tavily 检索分享内容` 开关，默认启用。

### v5.0.0
**🌌 沉浸感与高可用升级：生态联动与智能调度重构**

**1. 🌐 全域社交联动 (Qzone & 早报体系)**
*   **QQ空间生态打通**：深度联动 `astrbot_plugin_qzone`，支持 LLM 工具直接调用发说说。Bot 现在不仅能在群聊/私聊发声，还能拥有自己的“朋友圈”。
*   **独立早报速递**：新增【每天60s读懂世界】与【AI资讯快报】的独立定时任务。支持一键开启自动同步至 QQ 空间，支持 LLM 自然语言触发。
*   **资讯容错优化**：优化了 AI 资讯快报的获取逻辑，彻底解决源头无数据时推送空白图片的问题。

**2. 🎭 拟真化人设与认知增强**
*   **形象参考图 (图生图)**：完美适配新版 `astrbot_plugin_gitee_aiimg`，新增**形象参考图逻辑**。Bot 终于可以基于预设参考图生成生活照，保持长相和气质的绝对一致性！
*   **认知防呆机制 (第三人称修正)**：强制 LLM 进行“日程与昵称”交叉比对。若日程中包含用户昵称，自动转换为“和你一起”的第二人称语气，彻底杜绝“我今天要去和张三吃饭(对张三说)”的智障语病。
*   **动态记忆串联**：新增“过往动态记忆”参考，Bot 在生成新内容时会回顾最近发过的动态，避免车轱辘话，让生活轨迹更连贯。
*   **专属知识库自定义**：开放【知识】与【推荐】类型的内容库，用户可完全自定义话题标签池。

**3. ⏰ 工业级调度与可用性重构**
*   **千群千面独立调度 (高阶特性)**：重构了推送目标的解析引擎，现支持“三段式”高阶语法（`群/QQ号:独立Cron定时:专属类型` 或 `群/QQ号:专属类型`）。你可以为某些特定的群设置**完全独立的定时器和分享内容轮换**，彻底脱离全局规则！(例如：`123456:0 7 * * *:news,mood` 即可让该群仅在早7点按新闻、心情轮换)。
*   **防丢/防重的持久化调度**：重写定时器底座。将“随机触发时间段”和“Cron随机延迟”的生成结果统一存入 SQLite 数据库。即使 Bot 重启或崩溃，也能在启动时精准恢复未执行的延迟任务，**彻底解决重启导致的重复推送或跳过执行问题**。
*   **多时段随机阵列**：新增多时间段随机分享模式，支持统一设置或为独立群组设置专属的随机时间点，彻底告别机械的固定时间推送。
*   **LLM 自动降级熔断**：指定的大模型节点宕机或风控？系统会自动捕获并无缝降级到配置中的第一个可用默认模型，确保分享任务永不中断。

**4. ⚡️ 底层引擎与健壮性优化**
*   **深度回溯引擎重写**：采用全新的游标分页(SEQ)策略重写聊天历史获取逻辑，在群聊消息海量时抓取更稳定、更深层。
*   **插件发现机制强化**：优化了对其他插件（如 Gitee生图、Qzone）实例的扫描探测逻辑，完美兼容 AstrBot 在不同阶段的异步加载状态。
*   **海量热搜源扩充**：新增 夸克、36氪、51CTO、A站、爱范儿、网易、新浪、澎湃 8 大热搜源接入。
*   **常规维护**：修复了若干奇奇怪怪的底层 BUG。

### v4.0.0
**🚀 架构级升级：原生适配与性能重构**

**1. ⚡️ 异步数据库架构**
*   **重构**：引入数据库管理，全面重写底层存储逻辑。
*   **性能**：所有 SQLite 操作（读写历史、状态更新）均放入独立线程池执行。
*   **效果**：彻底杜绝了因数据库 I/O 导致的 Bot 主线程卡顿，即使历史记录达到数万条也能秒级响应。

**2. 🔗 原生上下文注入**
*   **对话历史注入**：适配 AstrBot 的上下文新接口。
    *   **变更前**：Bot 发送的每日分享是“孤立”的消息，不被 LLM 记忆，导致用户回复“展开讲讲”时 Bot 一脸懵逼。
    *   **变更后**：插件会伪造一组“隐形对话”（User: 请发送分享 -> Bot: [分享内容]）写入历史记录。
    *   **效果**：完美保持上下文连贯性，Bot 能基于刚才分享的内容继续与用户深入聊天。

**3. 👁️ 深度上下文感知**
*   **深度分页策略**：重写聊天历史消息获取逻辑。
    *   打破了仅获取最近 20 条消息的限制，现在支持**深度回溯**（默认24小时聊天历史消息）。
    *   采用分批轮询机制，确保在群聊消息量巨大时也能准确抓取上下文。
*   **群聊态势感知**：新增群聊热度分析算法。
    *   统计短时间内的有效消息数、活跃人数。
    *   计算 **聊天热度**：Bot 现在知道群里是“死寂”、“闲聊”还是“激烈讨论”，并据此调整开场白的打扰程度。

**4. 🤖 零配置 Bot 发现**
*   **智能初始化**：不再强制要求用户在配置文件中填写繁琐的平台适配器。
*   **逻辑**：插件启动时会自动扫描 AstrBot 加载的所有适配器，自动锁定可用的 Bot 实例。
*   **效果**：开箱即用，极大降低了新手配置门槛。

### v3.0.0
**🧠 核心升级：全链路 Agent 化**

**重构 配图生成逻辑：**
- 新增 `_agent_extract_visuals` 思考链路，引入 JSON 结构化提取。
- 智能仲裁“文案 vs 日程”的地点冲突，减少“图文不符”的幻觉。

**重构 选题与情感逻辑：**
- 新增 **选题 Agent**：结合历史记录“头脑风暴”出新颖的冷知识，自动去重。
- 新增 **情感分析 Agent**：基于全文理解判断 TTS 语音情感（happy/sad/angry）。

**🕒 细粒度时间管理**
- 新增 **“深夜 (Late Night)”** 时段（22-24点），文案强制带晚安，配图光影转为幽暗风格。

### v2.0.0 
**⚡️ 架构升级：异步任务流**
- 将生成与发送逻辑剥离为后台 `asyncio.create_task`，彻底解决因绘图/TTS耗时过长导致的 LLM 工具调用超时问题。
- **✨ 新增 AI 视频生成**：支持将生成的静态配图转换为 5秒 动态视频，让 Bot 的“早安”动起来。

### v1.0.0 
**🌱 初始发布**
- 支持 Cron 定时任务、时间段感知、多模态（图/文/音）内容生成、全网热搜聚合。

**多模态内容生成：**
- 📝 智能文案：基于 LLM 生成，集成 Life Scheduler（天气/穿搭/日程）和 聊天历史，拒绝尴尬开场。
- 🎨 智能配图：根据文案自动生成绘画提示词。支持从生活上下文中提取“穿搭”信息，智能判断是否需要人物出镜（画人 vs 画景）。
- 🗣️ 情感语音：集成 TTS 插件，根据分享类型和时间段自动注入情绪（happy/sad/neutral/angry）。
